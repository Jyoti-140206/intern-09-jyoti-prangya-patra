# prescriptions — Module 2 & 3: Prescription Records

**Implement here**
- Prescription upload, storage and metadata (doctor, issue date, expiry date)
- Linking extracted OCR results to prescription records
- Prescription expiry reminders (raises events consumed by `notifications`)

**Expected files:** `models.py`, `serializers.py`, `views.py`, `urls.py`, `migrations/`, `tests/`

**Milestone:** 2–3 (Week 3–6)
