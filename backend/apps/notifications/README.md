# notifications — Module 8: Smart Notifications & Alerts

**Implement here**
- Channel adapters: Firebase Cloud Messaging (push), Twilio (SMS), SendGrid (email)
- Real-time medicine reminders, refill alerts, missed-medicine notifications
- Emergency caregiver notifications
- Prescription expiry reminders
- Delivery status tracking and retry, admin-configurable notification settings

Every provider credential must come from environment variables — never commit keys.

**Expected files:** `models.py`, `providers/`, `tasks.py`, `services/dispatcher.py`, `tests/`

**Milestone:** 2 (Week 3–4), extended in 3–4

